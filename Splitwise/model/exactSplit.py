from model.split import Split
from model.user import User
from abc import  ABC, abstractmethod


class ExactSplit(Split, amount):
    def __init__(self):
        super(user)
        self.amount = amount


